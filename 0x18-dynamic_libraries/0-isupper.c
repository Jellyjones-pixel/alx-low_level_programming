#include <stdio.h>
#include "main.h"
/**
 * _isupper - checks if the character is in uppercase
 * @c: a  character variable
 * Return: _isupper
 */

int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}

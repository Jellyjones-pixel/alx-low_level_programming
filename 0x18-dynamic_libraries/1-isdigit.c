#include <stdio.h>
#include "main.h"
/**
 * _isdigit - checks if the character is a number
 * @c: a  character variable
 * Return: _is digit
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

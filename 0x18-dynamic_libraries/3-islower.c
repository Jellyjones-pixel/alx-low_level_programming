#include "main.h"
#include <stdio.h>

/**
 * _islower - used to check if the character c is a lowercase
 * @c: a variable
 * Return: if its lower
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
